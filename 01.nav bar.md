```
<!-- nav bar  -->

<nav class="bg-white shadow-md">
    <div class="max-w-7xl mx-auto px-4">
        <div class="flex items-center h-16 gap-3">

            <!-- Logo -->
            <div class="text-xl font-bold text-blue-600 flex-shrink-0">
                <a href="#">MyWebsite</a>
            </div>

            <!-- Mobile Search (Logo & Hamburger er maje) -->
            <div class="flex-1 md:hidden">
                <input type="text" placeholder="Search..." class="w-full px-4 py-2 border border-gray-300 rounded-lg bg-gray-50 text-sm
                 focus:outline-none focus:ring-2 focus:ring-blue-500" />
            </div>

            <!-- Desktop Menu (Center aligned) -->
            <ul class="hidden md:flex space-x-8 font-medium mx-auto">
                <li><a href="#" class="text-gray-700 hover:text-blue-600">Home</a></li>
                <li><a href="#" class="text-gray-700 hover:text-blue-600">About</a></li>
                <li><a href="#" class="text-gray-700 hover:text-blue-600">Contact</a></li>
                <li><a href="#" class="text-gray-700 hover:text-blue-600">Services</a></li>
            </ul>

            <!-- Desktop Right Side -->
            <div class="hidden md:flex items-center space-x-3">

                <!-- Desktop Search -->
                <div class="relative">
                    <input type="text" placeholder="Search..." class="w-56 pl-4 pr-10 py-2 border border-gray-300 rounded-lg bg-gray-50 text-sm
                   focus:outline-none focus:ring-2 focus:ring-blue-500" />
                    <svg class="absolute right-3 top-2.5 w-4 h-4 text-gray-500" fill="none" stroke="currentColor"
                        stroke-width="2" viewBox="0 0 24 24">
                        <circle cx="11" cy="11" r="8"></circle>
                        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
                    </svg>
                </div>

                <button class="border border-blue-600 text-blue-600 px-4 py-2 rounded-lg hover:bg-blue-50">
                    Sign in
                </button>

                <button class="bg-blue-600 text-white px-4 py-2 rounded-lg hover:bg-blue-700">
                    Sign up
                </button>
            </div>

            <!-- Mobile Menu Button -->
            <button id="menuBtn" class="md:hidden text-gray-700 flex-shrink-0">
                <svg class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
            </button>

        </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="hidden md:hidden bg-white border-t">
        <ul class="flex flex-col px-4 py-4 space-y-3 font-medium">
            <li><a href="#" class="block text-gray-700 hover:text-blue-600">Home</a></li>
            <li><a href="#" class="block text-gray-700 hover:text-blue-600">About</a></li>
            <li><a href="#" class="block text-gray-700 hover:text-blue-600">Contact</a></li>
            <li><a href="#" class="block text-gray-700 hover:text-blue-600">Services</a></li>
        </ul>

        <div class="px-4 pb-4 flex gap-3">
            <button class="w-1/2 border border-blue-600 text-blue-600 py-2 rounded-lg">
                Sign in
            </button>
            <button class="w-1/2 bg-blue-600 text-white py-2 rounded-lg">
                Sign up
            </button>
        </div>
    </div>
</nav>


<script>
  const menuBtn = document.getElementById("menuBtn");
  const mobileMenu = document.getElementById("mobileMenu");

  // Toggle menu on icon click
  menuBtn.addEventListener("click", (e) => {
    e.stopPropagation(); // important
    mobileMenu.classList.toggle("hidden");
  });

  // Prevent click inside menu from closing it
  mobileMenu.addEventListener("click", (e) => {
    e.stopPropagation();
  });

  // Click anywhere outside → close menu
  document.addEventListener("click", () => {
    if (!mobileMenu.classList.contains("hidden")) {
      mobileMenu.classList.add("hidden");
    }
  });
</script>

```
